query Flashcard($randId: String!) {
  flashcard(randId: $randId) {
    randId
    title
    body
    isPublic
    isForkedByYou
    isFork
    difficulty
    tags {
      name
    }
    status
    stats {
      avgTime
      numAttempts
      lastSeenOn
    }
    creator {
      name
      profilePic
      username
    }
  }
}
